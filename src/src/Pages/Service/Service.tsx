import { kServices } from "../../Utils/constants";

export const Service = () => {
  return (
    <div className="flex flex-col w-full h-full">
      <p className="text-2xl text-red-600">{kServices}</p>
    </div>
  );
};
